import {pkgFileName,tsconfigName} from './constants';


export function tsOptionsResolver(project, options) {
  // work with paths relative to project root
  // always returns an array of module options
  // 1. if root folder contains package.json and tsconfig.json
  //
}
